apply plugin: 'groovy'
apply from: 'maven-publish.gradle'

repositories {
    jcenter()
    mavenLocal()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
    implementation 'cn.mydreamy.gradle.plugin:lib:1.0.0'
}